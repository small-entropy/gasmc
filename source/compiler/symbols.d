module compiler.symbols;

public enum ReservedSymbols 
{
  Comment = 35, // Символ обозначает, что строка - комментарий (#)
  Space = 32, // Символ окончания лексемы (пробел)
  Carret = 13, // Символ возврата каретки
}